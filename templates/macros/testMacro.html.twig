{% macro input(name, value, type, size) %}
    <input name="{{ name }}" value="{{ value }}" />
{% endmacro %}

{% macro input2(name, value, type, size) %}
    <input name="{{ name }}" value="{{ value }}" />
{% endmacro %}

{% macro input3(names, valueeess, type, size) %}
    <input name="{{ name }}" value="{{ value }}" />
{% endmacro %}